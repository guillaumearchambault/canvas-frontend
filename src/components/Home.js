import React, { useContext, useEffect } from "react";
import Box from "@material-ui/core/Box";

const Home = () => {
  return <Box pt={0}>Welcome home, testing my pipeline.</Box>;
};

export default Home;
